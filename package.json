{
  "name": "change-me",
  "main": "./target/index.js",
  "typings": "./target/index.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/change-me/change-me.git"
  },
  "version": "0.0.0",
  "scripts": {
    "compile": "rm -rf ./target/ && tsc",
    "test": "yarn run compile && node --enable-source-maps ./target/tests.js",
    "format": "prettier --write './**/*.{ts,tsx,js,json,md}' '.prettierrc'",
    "prepublishOnly": "yarn run test && yarn run compile"
  },
  "files": [
    "src",
    "target"
  ],
  "dependencies": {},
  "devDependencies": {
    "@types/node": "18",
    "prettier": "^3.1.0",
    "typescript": "^5.3.2"
  }
}
